# /etc/default/smallsocks

# smallSocks - Small SOCKS protocol server

# please add no extra whitespace to the "service_run" line below
# valid values are "true" and "false", without the quotes
server_run=false
